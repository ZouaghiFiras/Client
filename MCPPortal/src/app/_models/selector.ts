/**
 * Represents the selector options for assignment search.
 */
export enum Selector {
  RESULT,
  ALL,
  WORK_IN_PROGRESS,
  APPOINTMENT_VISITS,
  TO_VISIT,
  EXPERTISE,
  TO_CHECK,
  CANCELLED,
  CLOSED,
}
